/**
 * Represents the _Account_ model
 *
 * ```yaml
 * id: number;
 * name: string;
 * email: string;
 * password: string;
 * ```
 */
export interface Account {
  id: number;
  name: string;
  email: string;
  password: string;
}