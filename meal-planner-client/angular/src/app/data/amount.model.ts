/**
 * Represents the _Amount_ model
 *
 * ```yaml
 * wholeNumber?: number;
 * numerator?: number;
 * denominator?: number;
 * ```
 */
export interface Amount {
  wholeNumber?: number;
  numerator?: number;
  denominator?: number;
}