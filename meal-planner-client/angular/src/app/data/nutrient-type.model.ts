/**
 * Represents the _Nutrient Type_ model
 *
 * ```yaml
 * id: number;name: string;
 * foodId: number;
 * unitId: number;
 * ```
 */
export interface NutrientType {
  id: number;
  name: string;
  unitId: number;
}